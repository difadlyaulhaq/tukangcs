---
// src/pages/dashboard.astro
import { adminDb } from '../lib/firebase-admin';

// Get user data from middleware
const user = Astro.locals.user;

// Fetch user profile from Firestore
let userData = null;
if (user?.uid) {
  try {
    const userDoc = await adminDb.collection('users').doc(user.uid).get();
    userData = userDoc.data();
  } catch (error) {
    console.error('Error fetching user data:', error);
  }
}
---

<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard - TukangCS</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        color: #333;
      }
      
      .header {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
      }
      
      .logo {
        font-size: 1.5rem;
        font-weight: bold;
        color: white;
      }
      
      .logout-btn {
        background: rgba(255, 255, 255, 0.2);
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      
      .logout-btn:hover {
        background: rgba(255, 255, 255, 0.3);
      }
      
      .container {
        max-width: 1200px;
        margin: 2rem auto;
        padding: 0 2rem;
      }
      
      .welcome-card {
        background: white;
        border-radius: 12px;
        padding: 2rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
      }
      
      .welcome-title {
        font-size: 2rem;
        margin-bottom: 0.5rem;
        color: #333;
      }
      
      .welcome-subtitle {
        color: #666;
        margin-bottom: 1rem;
      }
      
      .user-info {
        background: #f8f9fa;
        padding: 1rem;
        border-radius: 8px;
        margin-top: 1rem;
      }
      
      .user-info h3 {
        margin-bottom: 0.5rem;
        color: #333;
      }
      
      .user-info p {
        margin-bottom: 0.25rem;
        color: #666;
      }
      
      .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
      }
      
      .feature-card {
        background: white;
        border-radius: 12px;
        padding: 2rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        text-align: center;
        transition: transform 0.3s ease;
      }
      
      .feature-card:hover {
        transform: translateY(-5px);
      }
      
      .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
      }
      
      .feature-title {
        font-size: 1.25rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
        color: #333;
      }
      
      .feature-description {
        color: #666;
        line-height: 1.6;
      }
      
      .coming-soon {
        background: linear-gradient(45deg, #ff6b6b, #ffa500);
        color: white;
        padding: 0.25rem 0.5rem;
        border-radius: 4px;
        font-size: 0.75rem;
        margin-left: 0.5rem;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <div class="logo">TukangCS</div>
      <button class="logout-btn" onclick="logout()">Logout</button>
    </div>

    <div class="container">
      <div class="welcome-card">
        <h1 class="welcome-title">Selamat Datang! 👋</h1>
        <p class="welcome-subtitle">Anda berhasil masuk ke dashboard TukangCS</p>
        
        {userData && (
          <div class="user-info">
            <h3>Informasi Profil</h3>
            {user && <p><strong>Email:</strong> {user.email}</p>}
            <p><strong>Nama UMKM:</strong> {userData.nama_umkm}</p>
            <p><strong>Sektor:</strong> {userData.sektor}</p>
            {userData.sosmed && userData.sosmed.length > 0 && (
              <p><strong>Platform Sosmed:</strong> {userData.sosmed.join(', ')}</p>
            )}
            <p><strong>Bergabung:</strong> {new Date(userData.createdAt).toLocaleDateString('id-ID')}</p>
          </div>
        )}
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">🤖</div>
          <h3 class="feature-title">AI Customer Service <span class="coming-soon">Coming Soon</span></h3>
          <p class="feature-description">
            Chatbot AI yang dapat menangani pertanyaan pelanggan secara otomatis 24/7
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📊</div>
          <h3 class="feature-title">Analytics Dashboard <span class="coming-soon">Coming Soon</span></h3>
          <p class="feature-description">
            Analisis mendalam tentang interaksi pelanggan dan performa bisnis Anda
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔗</div>
          <h3 class="feature-title">Multi-Platform Integration <span class="coming-soon">Coming Soon</span></h3>
          <p class="feature-description">
            Hubungkan WhatsApp, Instagram, Facebook, dan platform lainnya dalam satu dashboard
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">⚡</div>
          <h3 class="feature-title">Quick Responses <span class="coming-soon">Coming Soon</span></h3>
          <p class="feature-description">
            Template respon cepat untuk pertanyaan yang sering diajukan pelanggan
          </p>
        </div>
      </div>
    </div>

    <script>
      async function logout() {
        try {
          // Clear client-side data
          localStorage.removeItem('firebase_session');
          localStorage.removeItem('firebase_user');
          
          // Call server-side logout
          await fetch('/api/auth/signout');
          
          // Redirect to signin page
          window.location.href = '/signin';
        } catch (error) {
          console.error('Logout error:', error);
          // Still redirect even if there's an error
          window.location.href = '/signin';
        }
      }
    </script>
  </body>
</html>